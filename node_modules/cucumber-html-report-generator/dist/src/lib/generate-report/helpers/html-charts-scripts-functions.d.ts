import type { ChartData, ChartOptions, ChartTypeRegistry, Plugin } from 'chart.js';
import { Chart } from 'chart.js';
export declare class HtmlScriptsFunctions {
    document: Document;
    theme: string;
    constructor(theme: string);
    graphOptions: () => ChartOptions;
    writeValueInCenter: () => Plugin<keyof ChartTypeRegistry>;
    generateChart: (chartName: string, chartData: ChartData, graphOptions: ChartOptions, chartCenterValue: string) => Chart;
}
//# sourceMappingURL=html-charts-scripts-functions.d.ts.map